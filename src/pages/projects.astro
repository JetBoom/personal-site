---
import PageTitle from '@/components/PageTitle.astro'
import PageSubTitle from '@/components/PageSubTitle.astro'
import ListProjects from '@/components/Project/ListProjects.astro'
import { siteConfig } from '@/site.config'
import BaseLayout from '@/layouts/BaseLayout.astro'
import SectionWrapper from '@/components/SectionWrapper.astro'
import PageSection from '@/components/PageSection.astro'
import { getGitHubRepos } from '@/utils/github'

const { github } = siteConfig

const projects = await getGitHubRepos(github)

const title = 'Projects'
---

<BaseLayout title={title}>
    <SectionWrapper>
        <PageSection class="gap-4">
    		<PageTitle>{title}</PageTitle>
    		<PageSubTitle>All of my open source</PageSubTitle>

		    <ListProjects projects={projects} />
        </PageSection>
    </SectionWrapper>
</BaseLayout>

<script>
	import { animate } from 'motion'

	animate(
	    '.title',
	    { y: [80, 0], opacity: [0, 1] },
	    {
	        duration: 1.5,
	        opacity: { duration: 3 },

	        offset: [0, 0.55, 0.75]
	    },
	)
</script>
