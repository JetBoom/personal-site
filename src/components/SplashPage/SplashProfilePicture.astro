<div class="pfp" />

<style>
    .pfp {
		position: relative;
		outline: 2px solid var(--col-bg);
		z-index: 1;
		@apply size-32 md:size-64 shadow-md shadow-black bg-[url(/myself.jpg)] bg-[length:180%_180%] bg-center rounded-full transition-all hover:bg-[length:100%_100%];

        &::before {
            content: '';
            z-index: -1;
            position: absolute;
            border: 2px solid var(--col-fg);
            border-radius: 50%;
            animation: pfp-ring ease 10s infinite;
        }
    }

	@keyframes pfp-ring {
		0% {
			inset: 0;
			border-color: transparent;
			box-shadow: 0px 0px 4px white, 0px 0px 4px inset white;
			opacity: 0;
		}

		10% {
			inset: 0;
			border-color: var(--col-fg);
			box-shadow: 0px 0px 4px var(--col-primary), 0px 0px 4px inset var(--col-primary);
			opacity: 1;
		}
		
		50%, 100% {
			inset: -75%;
			border-color: var(--col-primary);
			box-shadow: 0px 0px 32px var(--col-primary), 0px 0px 32px inset var(--col-primary);
			opacity: 0;
		}
	}
</style>
